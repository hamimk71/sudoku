Project Title: 
Enhancing the sudoku.py so that it has more interactive graphics
It will run on an Android handphone.

Installation:
1) Clone the repository: 'github clone https://github.com/hamimk71/
2) Navigate to the project directory: 'sudoku'
3) Install dependencies: 'npm install' or 'pip install-r requirements.txtxt'
